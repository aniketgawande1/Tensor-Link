syntax = "proto3";

package chat;

message ChatMessage {
    string username = 1;
    string message = 2;
}

message ChatResponse {
     string reply = 1;
}

service ChatService {
    rpc SendMessage(ChatMessage) returns (ChatResponse);
}